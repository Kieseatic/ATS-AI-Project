{
  "name": "ats-ai-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'No build step needed for Python backend'",
    "start": "cd backend && python app.py",
    "start:backend": "cd backend && python app.py"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}